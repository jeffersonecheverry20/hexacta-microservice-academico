server:
  port: 8087

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hexacta?serverTimezone=UTC
    username: root
    password: root
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
